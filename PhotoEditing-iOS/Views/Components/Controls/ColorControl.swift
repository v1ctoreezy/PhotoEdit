import SwiftUI
import PixelEnginePackage

// Todo: Missing Saturation, Contrast
struct ColorControl: View {
    var body: some View {
        GenericFilterControl(config: ColorConfiguration())
    }
}
